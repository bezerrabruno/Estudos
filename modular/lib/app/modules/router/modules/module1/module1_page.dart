import 'package:flutter/material.dart';

class Module1Page extends StatelessWidget {
  const Module1Page({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return const Scaffold(backgroundColor: Colors.teal);
  }
}
