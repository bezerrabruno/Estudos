import 'package:flutter/material.dart';

class Module3Page extends StatelessWidget {
  const Module3Page({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return const Scaffold(backgroundColor: Colors.cyanAccent);
  }
}
